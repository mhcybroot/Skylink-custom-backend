package root.cyb.mh.attendancesystem.model.enums;

public enum PortHistoryEventType {
    LINK_UP,
    LINK_DOWN,
    SPEED_CHANGE,
    MAC_LEARNED,
    MAC_REMOVED,
    IP_CHANGED,
    USER_HOSTNAME_CHANGED,
    VLAN_CHANGED,
    ERROR_BURST,
    FLAP_DETECTED,
    POE_CHANGE,
    ADMIN_STATE_CHANGE,
    CONFIG_UPDATE
}
